project_id = "spb-t4nqv2ey577y489x"

# A função process-import valida a autenticação internamente: aceita o JWT do
# usuário (importação manual) OU a chave de integração do robô SAP (x-api-key).
[functions.process-import]
verify_jwt = false
